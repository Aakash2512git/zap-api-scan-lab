name: Manual OWASP ZAP Scan (Severity Report Only)

on:
  workflow_dispatch:
    inputs:
      target_url:
        description: "OpenShift route URL (example: https://myapp.apps.cluster.example.com/api/query)"
        required: true
        type: string

jobs:
  zap-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Show target
        run: |
          echo "🔍 Scanning target:"
          echo "${{ inputs.target_url }}"

      - name: Validate URL
        run: |
          echo "${{ inputs.target_url }}" | grep -E '^https?://' || exit 1

      - name: Run OWASP ZAP baseline scan
        uses: zaproxy/action-baseline@v0.10.0
        continue-on-error: true
        with:
          target: ${{ inputs.target_url }}

      - name: Generate vulnerability report (Low / Medium / High)
        run: |
          REPORT=/zap/wrk/zap_report.json
          OUTPUT=zap-vulnerability-summary.txt

          echo "OWASP ZAP Vulnerability Report" > $OUTPUT
          echo "Target: ${{ inputs.target_url }}" >> $OUTPUT
          echo "Generated on: $(date)" >> $OUTPUT
          echo "===================================" >> $OUTPUT
          echo "" >> $OUTPUT

          echo "🔴 HIGH SEVERITY" >> $OUTPUT
          echo "-----------------------------------" >> $OUTPUT
          jq -r '.site[].alerts[] | select(.riskdesc | startswith("High")) |
            "- Name: \(.alert)\n  URL: \(.instances[0].uri)\n  Description: \(.desc)\n"' $REPORT >> $OUTPUT || true

          echo "" >> $OUTPUT
          echo "🟠 MEDIUM SEVERITY" >> $OUTPUT
          echo "-----------------------------------" >> $OUTPUT
          jq -r '.site[].alerts[] | select(.riskdesc | startswith("Medium")) |
            "- Name: \(.alert)\n  URL: \(.instances[0].uri)\n  Description: \(.desc)\n"' $REPORT >> $OUTPUT || true

          echo "" >> $OUTPUT
          echo "🟡 LOW SEVERITY" >> $OUTPUT
          echo "-----------------------------------" >> $OUTPUT
          jq -r '.site[].alerts[] | select(.riskdesc | startswith("Low")) |
            "- Name: \(.alert)\n  URL: \(.instances[0].uri)\n  Description: \(.desc)\n"' $REPORT >> $OUTPUT || true

          echo "✅ Vulnerability report written to $OUTPUT"
          cat $OUTPUT

      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: zap-reports
          path: |
            zap-vulnerability-summary.txt
            /zap/wrk/zap_report.html
            /zap/wrk/zap_report.json
